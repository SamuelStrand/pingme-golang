# Copy to .env.local when running Go binaries on the host (go run ./cmd/api).
# Docker Compose keeps using .env with host "db".
DATABASE_URL=postgres://postgres:postgres@localhost:5433/pingme?sslmode=disable
